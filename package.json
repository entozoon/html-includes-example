{
  "name": "html-includes-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "html-includes --src src --dest dist",
    "compile-minify": "html-includes --src src --dest dist --minify",
    "compile-watch": "html-includes --src src --dest dist --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "html-includes": "^5.0.0"
  }
}
